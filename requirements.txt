# Python dependencies for Terraform AWS automation tools
# These are optional tools that can enhance your Terraform workflow

# AWS SDK for Python - useful for AWS automation alongside Terraform
boto3>=1.26.0

# Python wrapper for Terraform - allows running Terraform from Python
python-terraform>=0.10.0

# Terraform inventory plugin for Ansible
terraform-inventory>=0.9.0

# AWS CLI (Python version) - alternative to the Go version
awscli>=1.27.0

# Jinja2 - for template generation if needed
Jinja2>=3.1.0

# PyYAML - for YAML configuration files
PyYAML>=6.0
